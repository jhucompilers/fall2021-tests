PROGRAM const02;
  CONST N = 20;
  -- a named constant should be allowed as an array size
  VAR arr : ARRAY N OF INTEGER;
  VAR i : INTEGER;

BEGIN
  i := 0;
  WHILE i < N DO
    READ arr[i];
  END;

END.
